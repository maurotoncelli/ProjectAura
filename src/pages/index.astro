---
/**
 * Root index — 301 redirect to default language (/it/).
 * All content lives under /[lang]/ routes for proper SEO.
 */
import { DEFAULT_LANG } from '../i18n/index';
return Astro.redirect(`/${DEFAULT_LANG}/`, 301);
---
